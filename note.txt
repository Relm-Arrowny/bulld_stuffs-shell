add comtom exe.
custom_exe arg1 arg2

split_string

check if custome_exe is in path
    if command start with / and is executable set it to path and skill the check
        checkTypeDefaultPath(const char* input)
        checkTypePath(const char* path, const char* input)

if so excute it with arg
    fork()
    if pid ==0
        execv(path, argv)
    else
     waithpid(pid, &status, 0);
     free_string_list(path)